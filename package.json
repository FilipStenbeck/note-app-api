{
    "name": "note-app-api",
    "version": "0.0.1",
    "description": "Api for note app",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node index.js",
        "dev": "nodemon --ignore notesDB.json index.js",
        "docker:build": "docker build . -t note-app-api:latest",
        "docker:run": "docker run -d -p 4000:4000 -t note-app-api:latest"
    },
    "author": "Filip Stenbeck",
    "license": "ISC",
    "dependencies": {
        "apollo-server": "^2.9.7",
        "crypto": "^1.0.1",
        "graphql": "^14.5.8",
        "node-json-db": "^1.0.2"
    },
    "devDependencies": {
        "nodemon": "^1.19.4"
    }
}
